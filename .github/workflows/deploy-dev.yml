name: Deploy DEV

on:
  workflow_dispatch:
    inputs:
      service:
        description: DEV service to deploy manually during decommission
        required: true
        type: choice
        options:
          - api
          - portal
          - renderer

permissions:
  contents: read

jobs:
  guard-main:
    name: Require main branch
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Reject non-main dispatches
        shell: bash
        env:
          DISPATCH_REF: ${{ github.ref }}
        run: |
          set -euo pipefail
          if [[ "$DISPATCH_REF" != "refs/heads/main" ]]; then
            echo "DEV deployments must be dispatched from refs/heads/main; received $DISPATCH_REF." >&2
            exit 1
          fi

  deploy-selected-service:
    name: Deploy selected DEV service
    needs: guard-main
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/_deploy-cloud-run.yml
    with:
      service: ${{ inputs.service }}
      environment: development
      image_tag: git-${{ github.sha }}

  dev-deploy-passed:
    name: dev-deploy-passed
    if: always()
    needs:
      - guard-main
      - deploy-selected-service
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Verify guard and selected deployment
        shell: bash
        env:
          GUARD_RESULT: ${{ needs.guard-main.result }}
          DEPLOY_RESULT: ${{ needs.deploy-selected-service.result }}
        run: |
          set -euo pipefail
          if [[ "$GUARD_RESULT" != "success" ]]; then
            echo "Main-branch guard did not succeed: $GUARD_RESULT" >&2
            exit 1
          fi
          if [[ "$DEPLOY_RESULT" != "success" ]]; then
            echo "Selected DEV deployment did not succeed: $DEPLOY_RESULT" >&2
            exit 1
          fi
          echo "Selected DEV deployment passed."
